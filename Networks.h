

// list of known networks, so testing in different environments results in wifi connection w/o rewriting wifi setting every time
// list in order of prefered networks

const char *networks[][2] = 
{
  //{"network", "password"},
    {"NetworkSSID1", "Password1"},
	{"NetworkSSID2", "Password2"},
    {"NetworkSSID3", "Password3"}
};

